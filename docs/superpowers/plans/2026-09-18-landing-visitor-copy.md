# Plan: landing visitor copy (P2)

## Goal
Mobile full-width CTA + visitor copy sanity in docs/index.html.

## Research
research: skipped (no external unknowns; DESIGN.md responsive contract is the source)

## Tasks
1. Add mobile full-width rule for `.primary` in docs/index.html.
2. Sanity-scan visitor strings; fix only concrete jargon leaks.
3. Run `python scripts/check_release.py` and expect PASS.
4. Commit.

## Out of scope
og:image; chip values; CI.
